# ======================
# Better Auth Configuration
# ======================

# Required: Secret key for signing tokens and cookies
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Required: Base URL of your application (used for OAuth redirects, e.g. Google login).
# Local: http://localhost:3000
# Production: https://yourdomain.com (no trailing slash)
# In Google Cloud Console, add: {BETTER_AUTH_URL}/api/auth/callback/google as authorized redirect URI.
BETTER_AUTH_URL=http://localhost:3000

# ======================
# Database Configuration
# ======================

# Required: PostgreSQL connection URL (Supabase with connection pooling)
DATABASE_URL=

# ======================
# Google OAuth Configuration
# ======================

# Required for Google OAuth: Get from Google Cloud Console
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ======================
# Watson/IBM Cloud Configuration
# ======================

IBM_CLOUD_API_KEY=
WATSON_CLOUD_API_KEY=
WATSON_INSTANCE_API_URL=
# Optional: LLM for agents. If unset, first model from list-models API is used, or fallback groq/openai/gpt-oss-120b. Get ID from Watson UI → Profile → AI Model.
# WATSON_AGENT_LLM=

# Optional: AskOrchestrate (orchestrator) agent ID. If set, the Ask page sends users to this agent. If unset, the app resolves by display name "AskOrchestrate" from the agents list.
# WATSON_ORCHESTRATOR_AGENT_ID=

# ======================
# Other
# ======================

EFFERD_REGISTRY_TOKEN=
